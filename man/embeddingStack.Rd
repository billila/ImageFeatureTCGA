% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embeddingStack.R
\name{embeddingStack}
\alias{embeddingStack}
\title{Create an embedding stack from a ProvGigaList object}
\usage{
embeddingStack(
  con,
  levels,
  layer = "last_layer_embed",
  redownload = FALSE,
  parallel = FALSE,
  ...
)
}
\arguments{
\item{con}{\code{ProvGigaList} object containing multiple ProvGigaPath files or
URLs.}

\item{levels}{\code{character()} vector specifying the data level for each file in
the \code{ProvGigaList}. If not provided, levels will be inferred from the
\code{ProvGiga} objects within the list.}

\item{layer}{\code{character()} vector specifying the embedding layer to import
from each file. Default is \code{"last_layer_embed"} for "slide_level" and
ignored for "tile_level".}

\item{redownload}{\code{logical(1)} indicating whether to re-download cached
files.}

\item{parallel}{\code{logical(1)} indicating whether to use parallel processing.}

\item{...}{Additional arguments passed to the slide and tile import
functions.}
}
\value{
A named list of tibbles, where each list element corresponds to a
specific level (e.g., "slide_level", "tile_level") and contains the
combined embeddings from all files at that level.
}
\description{
This function imports and combines embeddings from multiple ProvGigaPath files
contained within a ProvGigaList object. It supports both slide-level and
tile-level embeddings.
}
\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
slide_urls <- getCatalog("provgigapath") |>
    dplyr::filter(level == "slide_level", Project.ID == "TCGA-UVM") |>
    dplyr::slice(1:3) |>
    getFileURLs()

ProvGigaList(slide_urls) |>
   embeddingStack(redownload = FALSE, parallel = FALSE)
\dontshow{\}) # examplesIf}
}
